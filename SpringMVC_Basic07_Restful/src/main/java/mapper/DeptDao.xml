<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!-- 
public interface EmpDao {
	// Mapper와 연동할 추상함수(CRUD)
	
	// 전체조회
	List<Emp> select();
	
	// 조건조회 
	Emp selectByEmpno(int empno);
	
	// 삽입
	int insert(Emp emp);
	
	// 수정
	int update(Emp emp);
	
	// 삭제
	int delete(int empno);
}
 -->
<mapper namespace="kr.or.kosa.dao.DeptDao">

	<select id="select" resultType="kr.or.kosa.dto.Dept">
		select deptno, dname, loc from dept
	</select>
	
	<select id="selectByDeptno" resultType="kr.or.kosa.dto.Dept">
		select deptno, dname, loc from dept where deptno=#{deptno}
	</select>
	
	<insert id="insert" parameterType="kr.or.kosa.dto.Dept">
		insert into dept(deptno, dname, loc) values(#{deptno},#{dname},#{loc})
	</insert>	
	
	<update id="update" parameterType="kr.or.kosa.dto.Dept">
		update dept set dname=#{dname} , loc=#{loc} where deptno=#{deptno}
	</update>
	
	<delete id="delete">
		delete from dept where deptno=#{deptno}
	</delete>
</mapper>